(function () {
  "use strict";
  window.__BRAND__ = {
    // Only field actually read by main.js (initWhatsapp). Price, product
    // name, etc. are set directly in index.html / lib/i18n.js — keep
    // this file to just what's live-wired so it can't drift out of
    // sync with the copy that's actually on the page.
    whatsappNumber: "+525642145001"
  };
})();
